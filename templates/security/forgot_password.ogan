{{ extend('layouts/base.ogan') }}

{{ start('body') }}
<div class="min-h-screen flex items-center justify-center relative overflow-hidden">
    <!-- Background gradient -->
    <div class="absolute inset-0 bg-gradient-hero"></div>

    <!-- Decorative blobs -->
    <div class="absolute top-10 left-10 w-64 h-64 rounded-full opacity-30 pointer-events-none" style="background: radial-gradient(circle, rgba(192,116,89,0.4) 0%, transparent 70%); filter: blur(40px);"></div>
    <div class="absolute bottom-20 right-10 w-48 h-48 rounded-full opacity-25 pointer-events-none" style="background: radial-gradient(circle, rgba(122,154,139,0.4) 0%, transparent 70%); filter: blur(40px);"></div>

    <!-- Card -->
    <div class="relative max-w-md w-full mx-4">
        <div class="craft-card p-8 sm:p-10" id="form-container" hx-boost="true" data-controller="scroll-reveal">
            <!-- Logo -->
            <div class="text-center mb-8">
                <a href="{{ path('index') }}" class="inline-flex items-center gap-3 mb-6">
                    <div class="w-12 h-12 rounded-xl bg-primary-10 flex items-center justify-center">
                        <svg class="w-6 h-6 stroke-primary" viewBox="0 0 24 24" fill="none" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M9.937 15.5A2 2 0 0 0 8.5 14.063l-6.135-1.582a.5.5 0 0 1 0-.962L8.5 9.936A2 2 0 0 0 9.937 8.5l1.582-6.135a.5.5 0 0 1 .963 0L14.063 8.5A2 2 0 0 0 15.5 9.937l6.135 1.581a.5.5 0 0 1 0 .964L15.5 14.063a2 2 0 0 0-1.437 1.437l-1.582 6.135a.5.5 0 0 1-.963 0z"/>
                        </svg>
                    </div>
                    <span class="font-handwriting text-2xl font-medium text-foreground">Les Créas d'Elf</span>
                </a>

                <h1 class="font-serif text-3xl font-semibold text-foreground mb-2">Mot de passe oublié</h1>
                <p class="text-muted">Entrez votre email pour recevoir un lien de réinitialisation</p>
            </div>

            {% if emailSent %}
            <div class="bg-accent-10 border border-accent text-accent px-4 py-3 rounded-xl mb-6">
                <p class="font-semibold flex items-center gap-2">
                    <svg class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20 6 9 17l-5-5"/></svg>
                    Email envoyé !
                </p>
                <p class="text-sm mt-1">Si un compte existe avec cette adresse, vous recevrez un email avec les instructions.</p>
            </div>
            {% endif %}

            {% form.render() %}

            <div class="mt-6 text-center text-sm">
                <a href="{{ path('login') }}" class="text-primary hover:text-primary/80 transition-colors inline-flex items-center gap-2">
                    <svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <path d="M19 12H5M12 19l-7-7 7-7"/>
                    </svg>
                    Retour à la connexion
                </a>
            </div>
        </div>
    </div>
</div>
{{ end }}
